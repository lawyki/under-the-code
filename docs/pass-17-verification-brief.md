# PASS-17 VERIFICATION BRIEF — Part IV (Ch13–15)

Authored by the bridge, 2026-08-09. First of the two verification passes that
stand in for the waived per-part owner reads (`BRIDGE.md` §1 amendment,
2026-08-06): Pass 17 ran under the owner recalibration but never got the
second read Passes 14 and 16 did, and its clearances are therefore unaudited.
Your job is to audit them. **You have licence to override** — the executing
pass's verdicts are evidence, not rulings.

**Context:** *Under the Code* is the owner's own published book
(under.atheric.eu, CC BY-NC 4.0); this is an approved editorial plan executed
on the author's behalf. Ch13–15 cover databases, cryptography, and security in
an educational register — the work is editorial, on existing published text.
Model: **Fable.** If the session drops to a weaker model mid-part, stop at the
next ledger.

## Read first, in this order

1. `CLAUDE.md`, then `BRIDGE.md`.
2. `UNDER.md` §4q — the ledger you are auditing — then §4n's and §4p's
   verification sub-entries (the method you are repeating), then §4g and §5.
3. `STATE.md`.

## The brief

**Scope: `part-4.html` (Ch13–15).** The method is the 14/16 verification
pattern:

1. **Deep read of all of Part IV by you**, as a student who wants to be
   gripped — the Part-IV recalibration (pedagogy first, facts second; the Ch7
   pickle coda is the register bar) applies to your judgment too.
2. **Independent per-chapter fresh readers** (pedagogy lens, web-enabled),
   blind to §4q's verdicts.
3. **Every flag adjudicated by you against an adversarial defense** — the
   verification passes have historically *declined* several of their own
   readers' flags; expect to.
4. **Re-verify the load-bearing §4q corrections against primary sources or by
   recomputation** — verify before fix; audits carry their own errors. At
   minimum: the fig-14-2 factoring claim (RSA-250 record), SLH-DSA as
   hash-based, the fig-14-10 Cloudflare claim, the RSA-vs-ECC key-growth
   direction, fig-14-3's reordering, Ed25519 2011 / SHA-3 2015,
   Bernstein/Junger, the IMS and Selinger fixes, projection-pushdown, B-tree
   1970, Codd eleven pages, Pwn2Own/ZDI, prompt injection 2022, the Capital
   One split.
5. **Re-judge the four §4q re-carries** (ch13-algebra-p3, ch13-sql-p4,
   ch14-ecc-p4, ch15-defense-p1) against the pickle-coda bar, and **re-read
   the cleared spans** — the "would I enjoy reading this?" clearances are
   exactly the unaudited part.
6. Fix what fails, with the same confirmation gate over your own diff (the
   gate has caught the verifier's own errors in every prior verification).

**Re-check the declines, then leave the settled ones settled:** Sony PSN
(owner-aware through every gate since pass 7), "3 nm" (§4g owner-ruled),
Enigma "six years" (a Ch1 question), the ch15-culture directory (intentional
resource content). Overriding an owner ruling is not in your licence.

## Constraints

- Strongroom identity untouched — the serif-on-cream body is deliberate
  (§4d/§5). Heroes, chapter order, the ch14-tls-pqc-p9 stamp, thesis set and
  authorship line untouched.
- **Figures: text labels only**, §4g-precedented, a11y titles matched. A
  genuine *geometry* defect gets flagged to the Pass 19 closing sweep with
  coordinates — not fixed here (the two known Part V geometry defects belong
  to the Pass-18 verification, not you).
- **Anchor-ID law:** part-4 set byte-identical (verify against HEAD; last
  recorded 228) or `ANCHOR_ALIASES` migration verified at zero pixel delta.
- **Glossary:** regenerate; expect 520, no entry added or removed unless
  deliberate and named; diff the definitions; known extractor gotchas
  (em-dash clipping, term-cue junk, SKIP_WORDS precedents).
- British spelling; house voice.

## Verification & close-out

The full §7 protocol: local CF-mimicking server, Chromium **and** WebKit,
1440 + 375, part-4 / index / glossary, dark + light — 0 console errors,
0 external requests, 0 h-scroll, nav 48 px, anchors resolve; screenshot every
touched passage in both engines; push; **confirm live parity** with corrected
strings on the live page.

Ledger: a **"### Pass-17 verification"** sub-entry under `UNDER.md` §4q, in
the §4n/§4p verification shape — kept verbatim / re-carried / wrongly cleared
found & fixed / considered and declined / invariants / verification. Update
`STATE.md` (item 2 done; Pass-18 verification next). Record the commit sha and
the confirmed push. Flag anything the Pass 19 sweep should carry.
